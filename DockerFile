# Usamos la imagen oficial de OpenJDK 17 slim (ligera)
FROM openjdk:17-jdk-slim

# Directorio de trabajo dentro del contenedor
WORKDIR /app

# Copiamos el .jar generado por Maven al contenedor
COPY target/EverGrowth-server-0.0.1-SNAPSHOT.jar app.jar

# Exponemos el puerto (Render asignará uno dinámico)
EXPOSE 8080

# Variables de entorno recomendadas
ENV JAVA_OPTS=""

# Comando para ejecutar la aplicación
# Render asigna el puerto mediante la variable de entorno PORT
ENTRYPOINT ["sh", "-c", "java $JAVA_OPTS -Dserver.port=${PORT:-8080} -jar app.jar"]
